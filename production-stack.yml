version: "3"

volumes:
    PDSC_STACK_DB_VOLUME:
        driver: local

networks:
    pdscnet:

services:
    web:
        image: nginx:1.15.2-alpine
        deploy:
            replicas: 2
            restart_policy:
                condition: on-failure
            resources:
                limits:
                    memory: 256M
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - /srv/50words.online:/var/www/50words.online
            - /etc/letsencrypt:/etc/letsencrypt
            - $PWD/50words.online.nginx.conf:/etc/nginx/conf.d/50words.online.nginx.conf
        networks:
            - pdscnet
